machine:
  services:
    - docker

test:
    override:
        - (chmod +x setup_environment.sh)
        - (docker cp setup_environment.sh ubuntu:14.04:${HOME}/setup_environment.sh)
        - (docker run -t -i ubuntu:14.04 /bin/bash ${HOME}/setup_environment.sh)